apiVersion: v1
kind: Service
metadata:
  name: app-java-service
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: app-java
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP