apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-java-ingress
spec:
  rules:
    - host: app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-java-service
                port:
                  number: 80